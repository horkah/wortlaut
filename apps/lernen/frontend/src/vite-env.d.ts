/// <reference types="svelte" />
/// <reference types="vite/client" />

// Geteilte Komponenten liegen unter packages/ui und werden über den Alias
// `$ui` eingebunden (siehe vite.config.ts und tsconfig.json).
